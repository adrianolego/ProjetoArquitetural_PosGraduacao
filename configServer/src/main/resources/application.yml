
server:
  port: 8888

spring:
  application:
    name: configServer

#  cloud:
#    config:
#      server:
#        native: search-locations

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF
