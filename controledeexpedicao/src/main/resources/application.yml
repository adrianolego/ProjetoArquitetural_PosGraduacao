server:
  port: 8081

freteExterno:
  ribbon:
    listOfServers: http://localhost:7000

ribbon:
  ReadTimeout: 60000
  ConnectTimeout: 60000

config:
  #RabbitMQ message
  amq:
    hostname: localhost
    port: 5672
    username: guest
    password: guest

  atualizarExpedicao:
    exchange: atualizarExpedicao.exchange
    sendDLQQueue: atualizarExpedicao.sendDLQQueue
    sendQueue: atualizarExpedicao.sendQueue

  enviarExpedicao:
    exchange: enviarExpedicao.exchange
    sendDLQQueue: enviarExpedicao.sendDLQQueue
    sendQueue: enviarExpedicao.sendQueue


---
spring:
  profiles: docker-compose

freteExterno:
  ribbon:
    listOfServers: http://integrador:7000

config:
  #RabbitMQ message
  amq:
    hostname: rabbitmq
    port: 5672
    username: guest
    password: guest


